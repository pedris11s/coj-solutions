#include <bits/stdc++.h>

using namespace std;

int main()
{
    while(true)
    {
        int n;
        scanf("%d", &n);

        if(n == 42)
            break;

        printf("%d\n", n);
    }

    return 0;
}
