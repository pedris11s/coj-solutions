#include <bits/stdc++.h>

using namespace std;

int R, C, F, P;

int main()
{
    int a, b;
    cin >> a >> b;
    cout << a + b << "\n";

    cin >> C >> R >> P >> F;
  /*  for(int i = 0; i < P; i++)
    {
        int x, y;
        cin >> y >> x;
        TP[x][y]++;
    }

    for(int i = 0; i < F; i++)
    {
        int x, y;
        cin >> y >> x;
        TF[x][y]++;
    }

    for(int i = 1; i <= R; i++)
        for(int j = 1; j <= C; j++){
            TP[i][j] += TP[i - 1][j] + TP[i][j - 1] - TP[i - 1][j - 1];
            TF[i][j] += TF[i - 1][j] + TF[i][j - 1] - TF[i - 1][j - 1];
        }

*/

    return 0;
}
