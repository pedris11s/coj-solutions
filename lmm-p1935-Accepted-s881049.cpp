#include<bits/stdc++.h>

using namespace std;
   int a,b,sol;
int main ()
{
    while( scanf("%d%d",&a, &b) != EOF)
    {
          sol = a *(b * 2);
        printf("%d\n",sol);
    }
    return 0;
}
